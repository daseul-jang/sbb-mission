/* import { SetStateAction } from 'react';

type User = {
  id: string;
  password: string;
  email: string;
};

type Props = {
  title: string;
  type: string;
  user: User;
  onChange: () => void;
};

const input = {};

export default function AuthInput({ title, type, user, onChange }: Props) {
  return (
    <>
      <label className='text-sm font-bold text-gray-700 tracking-wide'>
        {title}
      </label>
      <input
        className='w-full text-base py-2 border-b border-gray-300 focus:outline-none focus:border-amber-400'
        type={type}
        placeholder='mail@gmail.com'
        value={user}
        onChange={onChange}
      />
    </>
  );
}
 */
